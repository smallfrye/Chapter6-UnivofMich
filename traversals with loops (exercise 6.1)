fruit = 'fruit'
index = 0
while index < len(fruit):
    letter = fruit[index]
    print letter
    index = index + 1

print 'The while loop is done!'

# similar while loop that executes and prints characters of the string in reverse
# exercise 6.1


fruit = 'fruit'
index = 0
while index > len(fruit)*-1:
    letter = fruit[index-1]
    print letter
    index = index - 1

print 'The while printed backwards!'
